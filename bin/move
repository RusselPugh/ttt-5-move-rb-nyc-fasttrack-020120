#!/usr/bin/env ruby

require_relative '../lib/move.rb'

# Code your CLI Here
board = Array.new(9, " ")
puts "Welcome to Tic Tac Toe!"
Puts "Where would you like to go?"

user_input = gets.strip
index = index_to_input(user_input)
move(board, index)
display_board(board)


